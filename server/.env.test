# 测试专用环境变量：deno task test 通过 --env-file 加载，VSCode Testing 面板通过 deno.envFile 加载。
# NODE_ENV=test 触发 prisma 从生产库复制快照到 DATABASE_URL 指向的测试副本。
NODE_ENV=test
DATABASE_URL=file:data/data.test.db
DATABASE_URL_PROD=file:data/data.db
